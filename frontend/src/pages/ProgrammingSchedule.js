import useGetDataOnMount from "../hooks/useGetData";

const ProgrammingSchedule = () => {
  // get the list of weeks
  // const [weeks] = useGetDataOnMount({
  //   url: 
  // })
  // get list of periods
  // get list of activitie
  return (
    <>
      <h1>List of Weeks</h1>
      <ul>

      </ul>
    </>
  )
}

export default ProgrammingSchedule;
